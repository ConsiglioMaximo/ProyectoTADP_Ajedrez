import Ajedrez.*

describe "test de creacion de casilleros" {
  test "description" {
    const tablero = new Tablero()
    tablero.crearCasilleros()
    assert.equals(64,tablero.cantidadDeCasillas())
  }
describe "probando github" {
  test "description" {
    const tablero = new Tablero()
    tablero.crearCasilleros()
    assert.equals(64,tablero.cantidadDeCasillas())
  }

}
